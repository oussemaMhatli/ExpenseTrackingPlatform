import { TrieMontantPipe } from './trie-montant.pipe';

describe('TrieMontantPipe', () => {
  it('create an instance', () => {
    const pipe = new TrieMontantPipe();
    expect(pipe).toBeTruthy();
  });
});
